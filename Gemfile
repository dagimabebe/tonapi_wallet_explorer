source 'https://rubygems.org'

ruby '3.1.2'

gem 'httparty'    # HTTP requests
gem 'json'        # JSON parsing
gem 'csv'         # CSV parsing/generation
gem 'rspec'       # Testing framework
gem 'dotenv'      # Environment variables management
